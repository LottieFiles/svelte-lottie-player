import { writable } from 'svelte/store';

export const autoplay = writable();
export const background = writable();
export const controls = writable();
export const loopCount = writable();
export const defaultFrame = writable();
export const direction = writable();
export const hover = writable();
export const loop = writable();
export const mode = writable();
export const speed = writable();
export const state = writable();

export const instance = writable();
export const animationData = writable();
export const frame = writable();
export const progress = writable();
